export const generateSettings = (settings: Record<string, string>) => ({
  payment_type: settings?.payment_type,
  instagram_url: settings?.instagram,
  facebook_url: settings?.facebook,
  twitter_url: settings?.twitter,
  referral_active: settings?.referral_active,
  otp_expire_time: settings?.otp_expire_time,
  customer_app_android: settings?.customer_app_android,
  customer_app_ios: settings?.customer_app_ios,
  delivery_app_android: settings?.delivery_app_android,
  delivery_app_ios: settings?.delivery_app_ios,
  vendor_app_android: settings?.vendor_app_android,
  vendor_app_ios: settings?.vendor_app_ios,
  group_order: settings?.group_order,
  footer_text: settings?.footer_text,
  ui_type: settings?.ui_type,
  address_text: settings?.address,
  phone: settings?.phone,
  email: settings?.email,
  reservation_time_durations: settings?.reservation_time_durations,
  reservation_before_time: settings?.reservation_before_time,
  min_reservation_time: settings?.min_reservation_time,
  active_parcel: settings?.active_parcel,
  before_order_phone_required: settings?.before_order_phone_required,
  reservation_enable_for_user: settings?.reservation_enable_for_user,
  social_auth_google: settings?.social_auth_google,
  social_auth_apple: settings?.social_auth_apple,
  social_auth_facebook: settings?.social_auth_facebook,
});
